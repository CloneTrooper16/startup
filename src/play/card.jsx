import React from "react";

import "./card.css"

export function Card({ piece, move}) {
    // const pieceIMG = piece != "none" ? "https://images.chesscomfiles.com/chess-themes/pieces/neo/150/" + piece.color + piece.type + ".png" : "";

    return (
        <div className="card">
            <img/>
        </div>
    );
}